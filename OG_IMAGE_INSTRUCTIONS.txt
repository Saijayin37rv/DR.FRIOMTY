INSTRUCCIONES: Generar imagen optimizada para `og:image` (recomendado 1200x630, JPG, calidad 80-85)

1) Usando ImageMagick (Windows - PowerShell / CMD):
magick convert instalacion_finalclima.jpg -resize 1200x630^ -gravity center -extent 1200x630 -strip -quality 85 assets/og-image.jpg

2) Nombre recomendado del archivo final: assets/og-image.jpg

3) Publicar en Hostinger:

  - Confirma que `assets/og-image.jpg` está en el repo y commitea/pushea los cambios.
  - El sitio es HTML estático: despliega con Git en hPanel (Advanced > Git), no como "Node.js Web App".

4) Actualiza en `index.html` las meta tags `og:image` y `twitter:image` con la URL absoluta de tu dominio:
  https://drfriomty.online/assets/og-image.jpg

5) Validación: usa el Debugger de Facebook (Sharing Debugger) y el Card Validator de Twitter para comprobar que la imagen se muestra correctamente.
